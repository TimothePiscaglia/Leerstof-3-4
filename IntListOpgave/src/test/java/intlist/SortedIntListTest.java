package intlist;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class SortedIntListTest {
/*
    SortedIntList list;

    private void addAllValues(){
        list.add(5);
        list.add(2);
        list.add(1);
        list.add(8);
    }

    @BeforeEach
    public void setup(){
        list = new SortedIntList();
    }

    // Tests zelfde als gewone IntList
    @Test
    void findMin_OnEmptyList_ReturnsNull() {
        // Act
        final Integer min = list.findMin();
        // Assert
        assertNull(min);
    }
*/
/*
    @Test
    void findMin_WithAddedValues_ReturnsLowest1() {
        // Arrange
        addAllValues();
        // Act
        final Integer min = list.findMin();
        // Assert
        assertEquals(1, min);
    }
*/
/*
    //Andere functionaliteit
    //getallen worden gesorteerd bijgehouden
    @Test
    void remove_WithValuesAdded_AllSortedValuesReturnedInOrder() {
        // Arrange
        addAllValues();
        // Act
        // Assert

        //TODO check if the values are removed in the right order (=sorted)
    }
*/
}